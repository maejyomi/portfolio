import React from "react";

const SubTitle = ({ title }) => {
  return (
    <div className="text-5xl font-bold font-archivo mt-5 tracking-tighter underline decoration-orange-300">
      {title}
    </div>
  );
};

export default SubTitle;
